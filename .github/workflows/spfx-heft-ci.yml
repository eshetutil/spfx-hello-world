name: SPFx CI (Heft / Node 22)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: spfx-hello-world/package-lock.json

      - name: Verify Node & npm versions
        run: |
          node -v
          npm -v

      - name: Install dependencies
        run: npm install
        working-directory: spfx-hello-world

      - name: Build SPFx (Heft)
        run: npm run build
        working-directory: spfx-hello-world

      - name: Package SPFx Solution
        run: npm run package-solution
        working-directory: spfx-hello-world

      - name: Upload SPFx package
        uses: actions/upload-artifact@v4
        with:
          name: spfx-sppkg
          path: spfx-hello-world/sharepoint/solution/*.sppkg
